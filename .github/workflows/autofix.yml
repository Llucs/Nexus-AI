name: Auto Fix Kotlin + PR

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  fix-and-pr:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Fix Gradle permissions
        run: chmod +x gradlew

      - name: Clean Gradle wrapper cache (fix permission bug)
        run: rm -rf ~/.gradle/wrapper

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Run ktlint format
        run: ./gradlew ktlintFormat

      - name: Create Pull Request with changes
        uses: peter-evans/create-pull-request@v6
        with:
          commit-message: "chore: auto format Kotlin code"
          title: "Auto fix Kotlin formatting"
          body: "PR automático com correções do ktlint"
          branch: auto-fix/kotlin-format
          base: main
          add-paths: |
            .